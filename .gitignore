# Dependencies
node_modules/
skills/*/node_modules/

# Build output
dist/

# Runtime secrets & identity data (never commit)
.env
identities/

# SSP session (runtime secrets & logs)
ssp-session/

# Process files
ragent.pid
ragent.log

# MCP server config (may hold secrets in server env)
mcp.json

skills/*

# macOS
.DS_Store
